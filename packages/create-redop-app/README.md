# create-redop-app

Scaffold a Bun-first Redop app with transport and deployment presets.

## Usage

```sh
bun create redop-app
```

Or run the CLI directly:

```sh
create-redop-app --name my-redop-app --transport http --deploy railway
```
